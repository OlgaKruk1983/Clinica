<form method="post" action="{{ path('app_citas_delete', {'id': cita.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cita.id) }}">
    <button class="btn">Delete</button>
</form>
